import React from 'react'
import styles from './page.module.css';

const page = () => {
  
  return (
    <main className={styles['mission-main']}>
      <h2 className={styles['mission-title']}>Mission</h2>
      <p className={styles['mission-text']}>
        The navigation system provides a set of screen elements that allow the
        user to move page to page through the website. The navigation design
        should communicate the relationship between the links it contains. The
        navigation system provides a set of screen elements that allow the user
        to move page to page through the website. The navigation design should
        communicate the relationship between the links it contains. The
        navigation system provides a set of screen elements that allow the user
        to move page to page through the website. The navigation design should
        communicate the relationship between the links it contains.
        </p>
    </main>
  )
}

export default page